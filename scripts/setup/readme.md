# Environnement de développement

### Exécuter le script d'installation
Le script peut être exécuté avec la commande suivante:
```bash
# Depuis scripts/setup
bash setup.sh
```

Ce script installera les pre-commits et les packages python correspondants à la version d'Airflow mise en production.
